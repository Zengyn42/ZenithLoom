# 核心框架（状态机 + 持久化）
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=3.0.0
langchain-core>=0.3.0    # BaseMessage, HumanMessage, AIMessage, add_messages

# 接口层
discord.py>=2.3.0        # P0: Discord 远程
libtmux>=0.28.0          # P1: Tmux session 管理

# 工具
python-dotenv>=1.0.0

# 注意：无需 langchain-anthropic 或 langchain-google-genai
# Claude 和 Gemini 通过本地 CLI subprocess 调用
